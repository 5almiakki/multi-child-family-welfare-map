spring:
  application:
    name: dagagomap
  jpa:
    open-in-view: false
  threads:
    virtual:
      enabled: true

custom:
  kakao:
    local:
      semaphore-limit: 30
  public-data:
    page-size: 10
